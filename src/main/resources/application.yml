spring:
  datasource:
    url: jdbc:postgresql://xxx:5432/demo
    username: xxx
    password: xxx
  redis:
    host: xxx
    port: 6379
    password: xxx
    database: 0
    lettuce:
      pool:
        max-idle: 16
        max-active: 32
        min-idle: 8

mybatis:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.diantongren.model
logging:
  level:
    com:
      diantongren: debug
